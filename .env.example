# Copy this file to .env before running docker compose up.
#   cp .env.example .env
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme-demo-pw

# Used by loader/load.py (defaults match the values above; override if you change them)
NEO4J_URI=bolt://localhost:7687
